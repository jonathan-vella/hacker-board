{
  "name": "hacker-board",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Serverless microhack scoring dashboard — Azure Static Web Apps + managed Functions + Table Storage",
  "engines": {
    "node": ">=20.0.0"
  },
  "scripts": {
    "start": "swa start src --api-location api",
    "build": "echo 'No build step for vanilla JS'",
    "test": "cd api && npm test",
    "test:e2e": "playwright test",
    "lint": "echo 'Add ESLint configuration'"
  },
  "devDependencies": {
    "@azure/static-web-apps-cli": "^2.0.0",
    "@playwright/test": "^1.58.2"
  },
  "overrides": {
    "cookie": "^1.1.0",
    "glob": "^13.0.0",
    "inflight": "npm:@npmcli/fs@4.0.0",
    "rimraf": "^6.0.0",
    "sudo-prompt": "npm:@vscode/sudo-prompt@^9.3.1",
    "tmp": "^0.2.5"
  }
}
